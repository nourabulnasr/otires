
  # Temporary Landing Page Design

  This is a code bundle for Temporary Landing Page Design. The original project is available at https://www.figma.com/design/O0LOZupmZb1GSpf0S8hgl7/Temporary-Landing-Page-Design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  